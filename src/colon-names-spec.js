'use strict'

/* eslint-env mocha */
const colonNames = require('.')

describe('colon-names', () => {
  it('write this test', () => {
    console.assert(colonNames, 'should export something')
  })
})
